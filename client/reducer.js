/* const initialStatePlaceholder = {
  placeholder: ['', '']
}

const PortfolioReducer = (state = initialStatePlaceholder, action) => {
  switch (action.type) {
    case 'Placeholder':
      return {
        placeholder1: [...state.portfolio, action.portfolio]
      }

    default:
      return state
  }
}

export default portfolioReducer
 */
